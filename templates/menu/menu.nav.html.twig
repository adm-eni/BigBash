<div
        class="sticky top-0 z-40 w-full backdrop-blur flex-none transition-colors duration-500 lg:z-50 lg:border-b lg:border-slate-900/10 border-slate-50/[0.06] supports-backdrop-blur:bg-white/60 bg-transparent">
    <div class="max-w-8xl mx-auto">
        <div class="py-3 border-b border-slate-900/10 lg:px-8 lg:border-0 lg:border-slate-300/10 mx-4 lg:mx-0">
            <div class="relative flex items-center">

                <a href="{{ path('outing_public_list') }}"
                   class="flex items-center text-lg mix-blend-lighten">
                    <h1 class="font-bold">2or</h1>
                </a>
                <div class="relative hidden lg:flex items-center ml-auto">
                    <nav class="leading-6 font-semibold mix-blend-lighten">
                        <ul class="flex space-x-8">
                            <li><a href="{{ path('city_list') }}">Villes</a></li>
                            <li><a href="{{ path('campus_list') }}">Campus</a></li>
                            <li><a href="{{ path('outing_private_list') }}">Accueil</a></li>
                            {% if app.user %}
                            <li><a href="{{ path('user_profile') }}">Mon profil</a></li>
                            <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>
                            {% else %}
                            <li><a href="{{ path('app_login') }}">Se connecter</a></li>
                            {% endif %}
                            {% if app.user %}
                            <li>Bonjour, <a class="font-bold" href="{{ path('user_profile') }}">{{ app.user.username }}</a></li>
                            {% endif %}
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</div>
