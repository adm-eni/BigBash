<div
        class="sticky top-0 z-40 w-full backdrop-blur flex-none transition-colors duration-500 lg:z-50 lg:border-b lg:border-slate-900/10 border-slate-50/[0.06] supports-backdrop-blur:bg-white/60 bg-transparent">
    <div class="max-w-8xl mx-auto">
        <div class="py-4 border-b border-slate-900/10 lg:px-8 lg:border-0 border-slate-300/10 mx-4 lg:mx-0">
            <div class="relative flex items-center">

                <a href="{{ path('outing_list') }}"
                   class="flex items-center text-lg text-fuchsia-300 hover:text-fuchsia-200">
                    <h1 class="font-bold">2or</h1>
                </a>
                <div class="relative hidden lg:flex items-center ml-auto">
                    <nav class="text-sm leading-6 font-semibold text-fuchsia-300">
                        <ul class="flex space-x-8">
                            <li><a class="hover:text-fuchsia-200" href="">Villes</a></li>
                            <li><a class="hover:text-fuchsia-200" href="">Campus</a></li>
                            <li><a class="hover:text-fuchsia-200" href="{{ path('outing_list') }}">Accueil</a></li>
                            {% if app.user %}
                            <li><a class="hover:text-fuchsia-200" href="{{ path('user_profile') }}">Mon profil</a></li>
                            <li><a class="hover:text-fuchsia-200" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                            {% else %}
                            <li><a class="hover:text-fuchsia-200" href="{{ path('app_login') }}">Se connecter</a></li>
                            {% endif %}
                            {% if app.user %}
                            <li>Bonjour, <a class="hover:text-fuchsia-200 font-bold" href="{{ path('user_profile') }}">{{ app.user.pseudo }}</a></li>
                            {% endif %}
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</div>
